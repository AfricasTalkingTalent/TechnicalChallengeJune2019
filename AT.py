#define a method finddocs that reads and applies the lambda function to each value in the list.

def finddocs(wfiles):
	f = open(x, 'r')
    return list(filter(lambda x:'a' in f.read(), wfiles))

my_function(['wdocuments','hw1_test.dat'])
